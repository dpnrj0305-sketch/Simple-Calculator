# Simple-Calculator
This project is a simple Python-based calculator that performs basic arithmetic operations like addition, subtraction, multiplication, and division. It takes user input, processes calculations, handles errors like division by zero, and runs continuously until the user exits.
